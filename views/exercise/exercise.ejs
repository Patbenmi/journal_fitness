<h1> List of Exercises</h1>

<div class="exerciseList">
<% exercise.forEach(function(exercise){ %>
    <% if(exercise.language == currentUser.language) {%>
      <h2><a href="/exercise/<%= exercise.id %>/"><%= exercise.name %></a></h2>
    <% } %>
  <% }) %> 
</div>